class TurboCopItem {
    constructor(options) {
        this.itemName = options.itemName;
        this.itemID = options.itemID;
        this.styleNames = options.styleName;
        this.styleIDs = options.styleID;
        this.sizeNames = options.styleName;
        this.sizeIDs = options.sizeID;
    }
}